# TripMemo 设计规则（DESIGN.md）

> **这份文件是全站视觉的「单一事实来源」。**
> 写样式前先查这里；这里没写的，就按第二节的三条原则推。
> 为什么需要它：之前视觉规则散落在 `PRD.md`、`TECH_DESIGN.md` 和 `css/style.css` 的注释里，
> 新加一个组件时**没有统一的尺子**，只能"照着旁边那个抄"，越抄越偏。

---

## 一、这份文件管什么、不管什么

| | 内容 |
|---|---|
| **管** | 颜色、字号、间距、圆角、阴影、动效、交互状态、响应式断点 |
| **不管** | 功能与交互流程（在 `PRD.md`）、技术选型与数据结构（在 `TECH_DESIGN.md`）、协作规则（在 `AGENTS.md`） |

参考来源：`web-design-layout` 技能的 `references/design-system.md`。
**哪里照搬、哪里改了、为什么改**，都写在对应小节里 —— 不照搬的部分比照搬的部分更值得记。

---

## 二、三条设计原则（冲突时以此为准）

**原则 1｜颜色只表达「类型」，不表达「时长」。**
地点类型四种：长期居住 / 短期停留 / 旅游 / 想去。颜色只用来区分它们。
停留时长**只由大小表达**（点直径、线宽、占比条）。违反这条 = 破坏 PRD 4.2 #1。

**原则 2｜同一套数值，在所有地方必须是同一个来源。**
一个值只定义一次（`--color-*` / `--text-*` / `--space-*` / `--radius-*`），任何地方都引用变量，不硬写。
**同一个值两种写法（如 `var(--radius)` 和硬写 `8px`）也算违规** —— 因为改的时候会漏一个。

**原则 3｜浅底不显脏。**
本项目是奶油浅底，任何"从深色设计搬过来的"手法都要按浅底重做色值（Day 8 加练已踩过两次）。
另：**铺满容器的浮层必须带 `pointer-events: none`**（本项目踩过 5 次）。

---

## 三、色板

### 3.1 背景三层（靠明度拉开层次，结构不要动）

| 变量 | 值 | 用途 |
|---|---|---|
| `--color-bg` | `#faf7f2` | 页面底色（奶油米白） |
| `--color-surface` | `#f4efe6` | 侧栏、面板、卡片头部（略深一档） |
| `--color-elevated` | `#ffffff` | 浮起层：弹窗、下拉、卡片主体（最亮） |
| `--color-hover` | `#ffffff` | **有色底**上的悬停高亮 |
| `--color-input` | `#fdfcfa` | **白弹窗内**的输入框 / 按钮底 |

> ⚠️ `--color-hover` 和 `--color-input` 都是"白"，但角色不同，不能合并：
> 前者用在米色侧栏上，后者用在白色弹窗里。合并之后必然有一处看不出边界。

### 3.2 文字色与对比度规则

| 变量 | 值 | 在页面底 | 在面板 | 在浮起白 | 判定 |
|---|---|---|---|---|---|
| `--color-text` | `#3a332b` | 11.64:1 | 10.86:1 | 12.44:1 | ✅ |
| `--color-text-weak` | `#746a5d` | 4.96:1 | 4.63:1 | 5.30:1 | ✅ |

**对比度规则（WCAG 2.1 AA）**：
- 普通文字（< 18px，或 < 24px 非粗体）：**对比度 ≥ 4.5:1**
- 大号文字（≥ 18px 粗体，或 ≥ 24px）：≥ 3:1

> ⭐ **一条容易搞反的常识：字号越小，越需要对比度，不是越不需要。**
> 之前 `--color-text-weak` 是 `#8a7f72`（3.67 / 3.42 / 3.92），**全部不达标**，
> 而它偏偏被用在 19 处、其中 12 处字号 ≤13px —— 小字配低对比度，是最差的组合。
> 已改为 `#746a5d`。

⚠️ **`--color-text-weak` 禁止压在 `--color-nav-active`（选中胶囊）上** —— 实测只有 4.13:1。
在这个底色上要用 `--color-accent-text` 或 `--color-text`。

⚠️ **新增一种底色配对时，必须重新测对比度**，不能假设"之前能用现在也能用"。

### 3.3 主色：⚠️ 它有三个角色，必须用三个变量

**这是本节最重要的一条。** `#b5762e` 当边框和当文字，需要的明度完全不同 ——
一个深色才够对比度，但用它当边框又太重。所以拆成三个变量：

| 变量 | 值 | 角色 | 允许用在哪 | 禁止用在哪 |
|---|---|---|---|---|
| `--color-accent` | `#b5762e` | **图形** | 边框、占比条、地图连线、类型色点、`::before` 装饰 | ❌ 不承载文字 ❌ 不当填充底 |
| `--color-accent-text` | `#8a531c` | **文字** | 主色文字（导航选中、chip 选中、标签、地点数胶囊、按钮悬停文字） | ❌ 不当大面积填充底 |
| `--color-accent-fill` | `#9c6325` | **填充底** | 主按钮 `.btn--primary` 的底色（上面压浅色文字） | ❌ 不当文字色 ❌ 不当边框 |

**实测（为什么非拆不可）**：

| 用途 | 值 | 最差场景 | 判定 |
|---|---|---|---|
| 图形（边框/线/条） | `#b5762e` | —（不承载文字，无需对比度） | — |
| 文字 | `#b5762e`（原） | 选中胶囊上 **2.93:1** | ❌ **不达标** |
| 文字 | `#8a531c` | 选中胶囊上 **4.91:1** | ✅ |
| 填充底 + `--color-on-accent` | `#b5762e`（原） | **3.61:1** | ❌ **不达标** |
| 填充底 + `--color-on-accent` | `#9c6325` | **4.79:1** | ✅ |

> ⚠️ 为什么填充底不能靠"把文字调亮"解决：`#b5762e` 配纯白也只有 **3.75:1** ——
> 只要底色不变，怎么调文字都过不了 4.5。**这种情况只能改底色。**

**主色相关的其余变量**：

| 变量 | 值 | 用途 |
|---|---|---|
| `--color-accent-fill-hover` | `#8a531c` | 主按钮悬停（与 `--color-accent-text` 同值，但语义不同，所以分开定义） |
| `--color-accent-bg` | `rgba(181, 118, 46, 0.12)` | 主色浅底（标签、胶囊、错误抬头底） |
| `--color-nav-active` | `#ece2d2` | 侧栏选中项底色 |
| `--color-on-accent` | `#fffaf3` | 主色**填充底**上的文字色 |

### 3.4 功能色与分隔线

| 变量 | 值 | 用途 | 对比度 |
|---|---|---|---|
| `--color-danger` | `#c0392b` | 删除按钮、必填星号、表单错误文字 | 5.44:1 on 白 ✅ |
| `--color-danger-bg` | `rgba(192, 57, 43, 0.1)` | 错误提示底 | — |
| `--color-border` | `rgba(58, 51, 43, 0.12)` | 分隔线、边框 | — |
| `--color-timeline-line` | `rgba(58, 51, 43, 0.2)` | 时间轴竖线 | — |

> ⚠️ **装饰性细线不能复用 `--color-border`。** 它的 alpha 只有 .12，
> 画成 1–2px 的线在奶油底上**肉眼几乎看不见**（Day 8 真实踩过）。
> 需要"看得见的装饰线"时，用 `--color-timeline-line`，或再定义一档。

### 3.5 玻璃拟态 token

| 变量 | 值 | 用途 |
|---|---|---|
| `--color-glass` | `rgba(255, 255, 255, 0.66)` | 地图上的浮层（搜索框、小按钮、筛选栏） |
| `--color-glass-strong` | `rgba(255, 255, 255, 0.82)` | 里面**要读文字**的浮层（下拉、空状态卡） |
| `--color-glass-sheen` | `rgba(255, 255, 255, 0.55)` | 顶部内高光（做出玻璃厚度） |
| `--glass-blur` | `16px` | 模糊半径 |
| `--glass-shadow` | `0 2px 10px rgba(58,51,43,.14), inset 0 1px 0 var(--color-glass-sheen)` | 玻璃投影 + 内高光 |

**规则**：
1. ⚠️ **模糊只有背景足够透明时才看得见。** 背景 alpha 超过 0.85 就等于"模糊白加"（Day 8 踩过：写了 `blur(8px)` 但底色是 `.92`）。
2. **每一处 `backdrop-filter` 都必须配 `-webkit-backdrop-filter`**（Safari 只认前缀版）。
3. **要长时间阅读文字的地方不做真玻璃**（错误面板、悬停提示、弹窗表单）—— 可读性优先于效果。
4. 用 `outline` 做焦点环，**不要用 `box-shadow`** —— `box-shadow` 是整体覆盖，会把玻璃的内高光和投影一起抹掉。

---

## 四、字体层级

### 4.1 字体栈

```css
font-family: -apple-system, BlinkMacSystemFont, system-ui,
  "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", sans-serif;
```

> 中文优先、西文自动回退（西文部分由系统选，不手动指定）。

### 4.2 字号阶梯（6 档文字 + 2 档图标）

| 变量 | 字号 | 用途 |
|---|---|---|
| `--text-xs` | **12px** | 标签、辅助元信息、缩略图占位提示、「展开 ▾」箭头 |
| `--text-sm` | **14px** | **界面正文**：按钮、输入框、列表正文、字段说明、卡片元信息 |
| `--text-base` | **16px** | **页面基准**（`body`）、卡片标题、弹窗标题、空状态主文案 |
| `--text-lg` | **18px** | 城市名、区块小标题 |
| `--text-xl` | **20px** | 年份标题 |
| `--text-2xl` | **28px** | 抽屉主视觉的大标题（Day 10 新增） |
| `--icon-size` / `--icon-size-lg` | **16px / 18px** | 导航图标（图标尺寸，不属于文字层级） |

> ⚠️ `--text-2xl` 是 Day 10 为「地点详情抽屉」加的一档。当时参考图里那块主标题
> 相当于 40px+，而我们的刻度盘最高只到 20px —— **20px 撑不住一个主视觉**。
> 加档时守住了两条：① 仍是"刻度盘里的一档"，不是随手写个 40px；
> ② **和上一档差得足够开**（20 → 28，差 8px）。

### 4.3 ⭐ 本项目的字号第一规则：相邻层级至少差 2px

技能的参考值用的是 **1.25 比例**（12 / 15 / 18.75 / 23.4…）。
**我们没有照搬**，理由是：我们的层级只有 5 档、字号范围只有 12–20px，
1.25 比例在前几步会算出 `15`、`18.75` 这种**相邻只差 2–3px 且带小数**的值；
而我们的实际问题是**相邻级差太小**——
之前 45 处 `font-size` 里有 **13 种取值**，其中 `13px` 和 `14px` 各出现 10 次，
**差 1px 的两个值占了近一半用量，肉眼根本分不出，只让"层级"变成了噪声。**

**所以规则是**：
1. **相邻层级至少差 2px**（相对差 ≥ 14%），保证肉眼可辨
2. **禁止 1px 递进**（如同时存在 13px 和 14px）
3. **禁止半像素字号**（`12.5px` / `13.5px` / `14.5px` —— 现在源码里三个都有，属违规）
4. **承载信息的正文不低于 14px**；只有**不承载信息的装饰性标记**（如「展开 ▾」「暂无照片」）才允许 12px
5. 12px 是**绝对下限**，不再往下 11px / 10px

### 4.4 行高

| 场景 | 行高 |
|---|---|
| 正文、段落（中文） | **1.7** |
| 界面控件（按钮、标签、表单项） | **1.4** |
| 标题 | **1.3** |

> 技能建议中文正文 1.7–1.8。我们取 **1.7** —— 1.8 在 14px 的卡片里会把卡片撑得很高、一屏放不下几张。

---

## 五、间距刻度

### 5.1 基于 4px 网格

| 变量 | 值 | 常用场景 |
|---|---|---|
| `--space-1` | 4px | 图标与文字的缝、胶囊内部 |
| `--space-2` | 8px | **元素默认间距**（按钮之间、色点与文字） |
| `--space-3` | 12px | **卡片内边距**、表单项间距 |
| `--space-4` | 16px | 卡片之间、网格 gap |
| `--space-5` | 20px | 面板内边距 |
| `--space-6` | 24px | **区块间距**、弹窗内边距 |
| `--space-8` | 32px | 大区块之间 |
| `--space-10` | 40px | 页面级留白 |
| `--space-12` | 48px | 页面底部留白 |

### 5.2 规则

1. **所有 `padding` / `margin` / `gap` 必须取刻度值。**
   禁用这些"随手值"：`2, 3, 5, 6, 7, 10, 11, 14, 15, 18, 22, 34`
   （现状：19 种取值，**57% 的用量不在网格上** → 这是"看着不精致"的主要来源之一）
2. **唯一例外：图形尺寸不算间距。**
   正圆直径（`.type-dot` 8px）、进度条高度（`.city-bar` 3px）、边框粗细、
   头像/缩略图尺寸 —— 这些是**图形的几何量**，不是留白，不要求落在 4px 网格上。

> 技能同时给了"4px 网格"和另一套以 rem 为单位的 `--space-*`。
> 我们**只取 4px 网格、并用 px 不用 rem**：本项目没有任何需要整体缩放的场景
> （不是要适配用户改浏览器根字号的阅读型网站），px 更直观、不会因为两层换算看错值。

---

## 六、圆角与阴影

### 6.1 圆角（按"元素体量"分档）

| 变量 | 值 | 用途 |
|---|---|---|
| `--radius-sm` | 4px | 小标签内部、小色块 |
| `--radius-md` | 8px | **按钮、输入框、面板**（最常用） |
| `--radius-lg` | 12px | **卡片**、弹窗内小卡 |
| `--radius-xl` | 16px | 大卡片、弹窗主体 |
| `--radius-2xl` | 24px | 预留（大容器） |
| `--radius-full` | 999px | **胶囊**（类型 chip、标签、计数胶囊） |
| （`50%`） | — | **只用于正圆**：地图色点、时间轴圆点、主城市锚点 |

**规则**：**禁止硬写数值**。禁止使用 `6 / 9 / 10 / 14px`。
⚠️ 现状里 `var(--radius)`（8px）和硬写 `8px` 并存 —— 同一个值两种写法，属违规。

### 6.2 阴影（4 档）

| 变量 | 用途 |
|---|---|
| `--shadow-sm` | 静止的卡片（几乎看不见，只是让它"贴"在底上） |
| `--shadow-md` | 卡片悬停（抬起来） |
| `--shadow-lg` | 浮层（下拉、悬停提示） |
| `--shadow-xl` | 弹窗 |

**规则**：阴影必须用暖棕灰 `rgba(58, 51, 43, α)`，**禁止纯黑** —— 纯黑压在奶油底上会发脏。

---

## 七、动效

| 变量 | 值 | 用途 |
|---|---|---|
| `--duration-fast` | 150ms | 悬停、颜色变化等微交互 |
| `--duration-normal` | 220ms | 卡片抬起、边框变化 |
| `--duration-slow` | 380ms | 入场动画（如城市卡片翻入） |
| `--ease-out` | `cubic-bezier(0.22, 0.68, 0.36, 1)` | 默认缓动（减速） |

**规则**：
1. **必须尊重 `prefers-reduced-motion`** —— 所有 `animation` 都要在 `@media (prefers-reduced-motion: reduce)` 里关掉。
2. **`animation` 配 `animation-delay` 时必须加 `animation-fill-mode: backwards`**，
   否则延迟期间元素会先以最终状态**闪现一下**再开始动（本项目踩过）。
3. **入场动画的延迟要有上限。** 依次入场时用 `Math.min(index, 8) * 55ms`；
   不设上限的话 30 个元素会一路加到 1.65 秒，最后几个等起来像卡死（本项目踩过）。

---

## 八、交互状态（每个可点元素必须齐 4 态）

| 状态 | 规范 |
|---|---|
| `:hover` | 边框转主色、底色转 `--color-hover`、或轻微上浮 |
| **`:focus-visible`** | ⭐ `outline: 2px solid var(--color-accent); outline-offset: 2px;` |
| `:active` | 按下时轻微回缩（`transform: translateY(1px)`）或底色加深一档 |
| `:disabled` | `opacity: 0.5; cursor: not-allowed;` |

### 8.1 ⭐ 一条比"焦点样式"更根本的规则：**可点 ≠ 可聚焦**

⚠️ 修复前的实际状况（Day 9 记录，留作教训）：
全站 `:hover` **13 处**，但 `:focus-visible` **0 处**、`:active` **0 处**、`:disabled` **0 处**。
而且问题比"没样式"更深一层 —— 必须**分成两类**看：

| 元素 | 实际标签 | 真实情况 |
|---|---|---|
| `.btn` `.chip` `.mini-btn` `.nav-btn` `.modal-close` | `<button>` | **原生可聚焦**，浏览器会画默认焦点环。问题不是"没有"，而是"没被主题化" |
| `.timeline-card` / `.place-row` | `<article>` / `<div>` | ⚠️ **不可聚焦、也没有 tabindex** → **键盘完全到不了**，这是真功能缺失 |
| `.search-item` | `<li>` | 同上，结果键盘**只能选第一条** |
| `.city-head` | `<summary>` | ✅ 原生可聚焦，本来就没问题 |

**规则：凡是给"非表单元素"挂 `click` 监听，必须同时补三样：**

1. `tabindex="0"` —— 能被 Tab 键走到
   ⚠️ **用 `0` 不用 `1/2/3`** —— 正数会打乱浏览器原生的 Tab 顺序
2. `role="button"` —— 读屏会念"按钮"，并提示可用空格/回车激活
3. `keydown` 处理 —— 把 `Enter` 和 **空格** 映射成同一个动作
   ⚠️ 空格必须 `preventDefault()`，否则页面会跟着往下滚

这条**最容易漏**，因为**鼠标用户永远遇不到它**，开发时自己也不会遇到 ——
没有任何东西会提醒你它缺了。但它正是"产品有没有真的做完"最容易验的一条。

### 8.2 焦点环怎么写

⭐ `outline: 2px solid var(--color-accent); outline-offset: 2px;`

**三条附加规则**：
1. 用 `:focus-visible` 而不是 `:focus` —— 前者**只对键盘生效**，鼠标点按钮不会留下一个圈。
2. **禁止只写 `outline: none` 而不给替代样式**（那等于把焦点提示删掉而不是改好）。
   输入框用"边框转主色"代替尖圈，是允许的替代方案。
3. **焦点环用 `outline`，不用 `box-shadow`** —— `box-shadow` 是整体覆盖，
   会和玻璃的内高光 / 卡片投影互相冲掉。

### 8.3 "不可聚焦但可选"的列表怎么办（combobox 模式）

搜索下拉是 `<ul role="listbox">` + `<li role="option">`，而 **`<li>` 不可聚焦**，
所以键盘焦点始终停在输入框上。"当前选到第几条"没法靠 `:focus` 表达，只能：

- 自己记一个 `activeIndex`
- 画到 DOM 上（`.is-active` 类 = 看得见的高亮）
- 同步给 `aria-activedescendant`（读屏知道是哪条）
- 输入框上同步 `aria-expanded`（下拉开着没）

⚠️ **本节的教训**：`aria-expanded` 一开始只在"关闭"函数里写了 `'false'`，
结果**开着的时候也报 `false`**，把读屏反过来误导了。
**一个状态分两处各写一半，必然漏一边 —— 状态的同步要集中在同一个函数里收尾。**

**方向键的取舍（由试用后决定）**：四个方向对称 —— 下/右 = 下一条，上/左 = 上一条。
⚠️ 代价：左右键在原生的文本输入框里是"移动光标"，现在被借去走列表了，
所以**下拉开着时没法用左右键挪光标**（按 Esc 收起，或用鼠标点）。
取舍理由：搜索关键词只有几个字，需要挪光标的场合很少；而"四个方向都能走列表"是很多人的下意识用法。
⭐ 另一条纪律：`preventDefault()` **只在有结果可走时才拦** ——
没有结果时左右键完全保持原生行为，不白抢按键。

---

## 九、响应式断点

**本项目是「桌面优先」（用 `max-width` 向下降级），不是技能的「移动优先」(`min-width`)。**
理由：TripMemo 是**桌面上的整理型工具**（看地图、翻时间轴、维护数据），
手机是"顺手看一眼"的次要场景。技能给的是营销落地页场景，主流量在手机。

| 断点 | 触发 | 变化 |
|---|---|---|
| **≥ 1024px** | 默认 | 左侧 189px 竖栏 + 主区左右布局 |
| **≤ 1023px** | 平板 / 小窗 | 侧栏降级为顶部 56px 横条；`body` 转 `column` |
| **≤ 767px** | 手机横屏 / 小平板 | 时间轴图片位 88→64px；地点网格转 1 列 |
| **≤ 559px** | 手机竖屏 | 导航只留图标；`app-title` 隐藏；时间轴卡片上下堆叠、藏圆点 |

**规则**：
1. **布局只在这些断点变化**。新增断点必须先登记到本表并说明理由。
2. ⚠️ 降级时必须**用同一份 HTML**（靠 CSS 改方向），不新增移动端专用结构 ——
   否则同一功能会有两套代码，改一处漏一处。
3. ⚠️ **"不溢出"要靠结构保证，不要靠调断点。**（Day 9 修复）
   顶部横条里装了「标题 + 3 个视图按钮 + 2 个动作按钮」，
   在 **560~720px** 这一段宽度不够 —— 而它原来是 `height: 56px` + **不换行**，
   会**直接把页面横向撑破**。
   修法是给 `.app-nav` 加 `flex-wrap: wrap` + `height: auto` + `min-height: var(--nav-height)`：
   **能一行就一行，装不下就换行** → 从结构上保证永不溢出，
   不需要去测一个精确的临界宽度（临界值还受浏览器缩放 / 显示缩放影响，调断点很容易调错）。
   ⚠️ 顺带一条：flex 子项默认 `min-width: auto` 会**拒绝收缩**，
      所以必须显式允许换行；只加 `overflow: hidden` 是把内容裁掉，比溢出更糟。

---

## 十、审查清单（逐条可核对）

以后每次改视觉，按这张表逐条打勾。**打不了勾的就是没做完。**

### 颜色
- [ ] 所有文字在它的**实际底色**上对比度 ≥ 4.5:1（大号文字 ≥ 3:1）
- [ ] 新增配色配对时**重新测过**对比度，没有沿用旧结论
- [ ] 主色按角色选对了变量（图形用 `--color-accent`、文字用 `--color-accent-text`、填充底用 `--color-accent-fill`）
- [ ] 装饰性细线没有复用 `--color-border`
- [ ] 阴影是暖棕灰，没有纯黑

### 字号与排版
- [ ] 字号只用了 `--text-xs` ~ `--text-xl` 五档，没有临时值
- [ ] **相邻层级至少差 2px**，没有 1px 递进
- [ ] 没有半像素字号
- [ ] 承载信息的正文 ≥ 14px
- [ ] 行高：正文 1.7 / 控件 1.4 / 标题 1.3

### 间距与形状
- [ ] 所有 `padding` / `margin` / `gap` 都落在 4px 网格上
- [ ] 圆角只用 `--radius-*`，没有硬写数值
- [ ] 没有"同一个值两种写法"

### 交互
- [ ] 新加的可点元素四个状态都齐（hover / focus-visible / active / disabled）
- [ ] **给非表单元素挂 `click` 时，补了 `tabindex="0"` + `role` + Enter/空格 的 `keydown`**（用 `0` 不用正数）
- [ ] 键盘操作时按 Tab 能把整页走一遍，且**看得见焦点在哪**
- [ ] 焦点环用 `outline`，没用 `box-shadow`
- [ ] 没有"只写 `outline: none` 不给替代"的地方
- [ ] 新增动画在 `prefers-reduced-motion` 里关掉了

### 响应式
- [ ] 在 1023 / 767 / 559 三个断点都看过，没有横向溢出
- [ ] 降级用的是同一份 HTML，没有新增移动端专用结构

### 工程习惯
- [ ] 铺满容器的浮层都带 `pointer-events: none`
- [ ] 用了 `display: flex/grid` 的元素同时处理了 `[hidden]`（浏览器的 `[hidden]` 会被盖掉）
- [ ] 每一处 `backdrop-filter` 都配了 `-webkit-backdrop-filter`
- [ ] **改完 css / js 后，`index.html` 里 10 个资源的 `?v=` 数字一起 +1**

---

## 附：本文档与上下游的关系

```
research.md      研究过什么、发现什么机会
   ↓
PRD.md           要做什么、什么算做完（33 条验收标准）
   ↓
TECH_DESIGN.md   用什么技术、数据怎么流
   ↓
DESIGN.md        ← 本文件：视觉怎么做、什么算合格
   ↓
css/style.css    真正的实现
```

**规则变化时以本文件为准并同步更新此处；实现与本文档冲突时，改实现，或先改本文档再改实现** ——
不允许"代码和文档各说各的"。
