# TripMemo（人生记忆地图）产品需求文档 · PRD

> 对应课程：Vibe Coding 五步工作流 第 ② 步「写 PRD」
> 上游依据：`research.md`（2026-09-17）
> 本期时间范围：**28 天**（Day 7 交付 MVP，Day 8–28 迭代）
> 本文件**不含技术选型**（框架、地图服务、存储方案属 Day 5），**不含代码**。

---

## 一、项目一句话定位

**TripMemo 是一个把「自己待过的地方」按停留时长画在地图上的个人记忆记录网站。**

- 面向**大众用户**，**不预设"旅行爱好者"身份**
- 核心不是"记录去过哪"，而是"**记录待了多久**"

---

## 二、目标用户与典型场景

### 2.1 目标用户（按优先级）

| 优先级 | 用户 | 特征 |
|---|---|---|
| **主要** | 生活轨迹稳定但**不常旅行**的人 | 学生、上班族。一年出游一两次，但人生里有大量有意义的地点 |
| 次要 | 有明确"补记过去"需求的人 | 想把自己长大的城市、住过的地方整理成一张图 |
| 再次 | 旅行爱好者 | 已有其他产品可用，不是我们的核心增量 |

### 2.2 典型使用场景

| # | 场景 | 用户此刻在想什么 |
|---|---|---|
| 1 | **补记过去** | "我大学四年是在这个校区度过的，得把这段补进去。" |
| 2 | **记录现在** | "刚搬进新住处，先记下来，以后回看会很有意思。" |
| 3 | **标注未来** | "一直想去这个地方，先标成'想去'，等以后点亮它。" |
| 4 | **回看与检索** | "我那几年到底住过几个地方？" |

---

## 三、要解决的问题（继承 research.md）

| # | 问题 | MVP 是否解决 |
|---|---|---|
| 1 | 现有产品只能记到**城市**层，对不旅行的人来说几乎无内容可记 | ✅ 解决（把颗粒度下切到"地点"，且不限于旅行） |
| 2 | 现有产品**都只能记录未来**，没有任何产品能补录过去 | ✅ 解决（手动录入 + 停留时段可填历史） |
| 3 | 地图上所有点**长得一样**，看不出"住四年"和"路过一下"的区别 | ✅ 解决（点的大小随停留时长变化） |
| 4 | **照片失去了它的坐标**——照片多了，忘记自己在哪拍的 | ❌ **本期不解决**（照片上传排在 Day 8–28） |

> 第 4 条虽然不是本期范围，但**数据字段中已为它预留位置**（见第六节），以保证后续加照片时不需要改数据结构。

---

## 四、功能范围

### 4.1 本期做（MVP，Day 7 交付）

**8 项功能。每项写清「用户动作」与「可观察结果」。**

| # | 功能 | 用户动作 | 可观察结果 |
|---|---|---|---|
| 1 | **地图打点** | ① 在地图上直接点击某处；或 ② 在搜索框输入地名并选中 | 地图上出现一个新点；所属城市被**自动填入**表单 |
| 2 | **地点类型** | 在新增/编辑时选一类：**长期居住 / 短期停留 / 旅游 / 想去** | 该点带上对应标识；筛选栏切换类型时，地图上只显示该类型的地点。**「想去」有三处特殊规则，见下方** |
| 3 | **点的视觉差异** | 无需操作（自动） | **停留时长越长，点越大**；鼠标悬停显示地点名与停留时长 |
| 4 | **回忆表单（＝ 5.4 的地点详情弹窗）** | 填写地点名称、所属城市、停留起止时间、类型、回忆正文、标签 | 保存后点开该地点，能看到全部已填内容；刷新页面内容仍在 |
| 5 | **城市分组** | 在地点列表视图里点击某个城市 | 展开 / 收起该城市下的地点列表；地图上该城市的点高亮 |
| 6 | **时间轴** | 滚动浏览 | 地点按停留开始时间**倒序**排成列表（最近的在最上，最早的在最下）。**不含「想去」类型** |
| 7 | **数据持久化** | ① 刷新页面 ② 关闭浏览器再打开 ③ 点「导出 JSON」 | ①② 数据仍在；③ 浏览器下载一个 `.json` 文件 |
| 8 | **地点连线（主城市放射）** | 设定**一个**主城市（任何城市都能被选为主城市） | 地图上从主城市向**每个其他城市**各连一条线；**线宽随该城市的累计停留时长变化**——住得越久线越粗 |

> **说明**：MVP 由 `research.md` 的 7 项扩为 8 项——保留「地点连线」，因为主城市放射方案解决了它的语义问题（详见 4.2 #2）。

#### 关于「想去」类型的特殊规则（2026-09-17 由 shy 确定）

「想去」的地方**还没去过**，数据上没有停留时长，所以这三处单独定：

| 项 | 规则 |
|---|---|
| **点的大小** | **固定为最小尺寸，不参与大小映射**（它的"分量"还没产生） |
| **悬停文案** | 显示「**下一站**」，**不显示时长** |
| **时间轴** | **不进入时间轴**（时间轴是"回看人生轨迹"，想去还没发生）。想去的地点只出现在**地图**与**地点列表**里 |

#### 关于「地点连线」的规则（2026-09-17 由 shy 确定，18:57 修订）

定为**放射状（hub-and-spoke）**，且**线本身也承载信息**：

- 主城市**只有一个**，**由用户自己选择**（任何城市都可以被设为主城市，作为"人生锚点"）
- 从主城市向**每个非主城市**画**一条**线——**按城市聚合，不是每个地点一条**（否则同城的线会叠成一束，线宽互相盖住）
- **主城市内的地点不连线**（否则线会全糊在主城中心，什么也看不出）
- **线宽 ∝ 该城市的累计停留时长**：住得越久、分量越重，线越粗；反之越细
- **线宽设上下限**，避免极端值把某条线粗成一条带（同"点直径有上下限"的逻辑）
- **「去的次数」不单独编码到线上**：该城市下有几个点，图上就摆着几个点，"去过几次"本身就看得见。**线专心表达"分量"**
- 线条**不使用颜色**（颜色已留给"地点类型"）；也不再需要虚实区分（只有一个主城市）
- **主城市设了、但该城市还没有任何地点时**：地图上显示一个**灰色锚点**；该城市有了第一个地点后，锚点**自动变为正常状态**并开始向其他城市画线
- 主城市可随时更换或取消，线**自动重算**

### 4.2 本期砍掉的功能（★ 本节为今日掌握目标）

**8 条砍项 + 1 条"复核后保留"（下表共 9 行）。每条写清：砍什么、藏在哪、为什么砍、保留什么。**

| # | 砍掉的部分 | 属于 | 为什么砍 | 保留了什么 |
|---|---|---|---|---|
| 1 | 点的**颜色**与**形状**编码 | 点的视觉差异 | 颜色须留给"地点类型"，否则两套视觉编码打架，用户分不清颜色含义 | **大小随停留时长变化** |
| 2 | ~~地点连线~~ → **不砍**（改为保留，见 4.1 #8） | — | 主城市放射方案解决了"连线语义不清"的问题 | 放射连线 |
| 3 | 回忆正文的**富文本**（加粗、列表、链接） | 回忆表单 | 富文本要引入编辑器组件，工作量接近整个表单；回忆是"写给自己看的一段话" | 纯文本，支持换行 |
| 4 | **标签管理**（重命名/合并/删除）与**按标签筛选** | 回忆表单 | MVP 数据量只有几十个地点，筛选没有使用场景 | 标签的**添加**与**显示** |
| 5 | 时间轴的**按年/月折叠与筛选** | 时间轴 | 需分组逻辑与交互；数据量小时看不出价值 | 单一倒序列表 |
| 6 | 城市层级的**统计数字**（该城地点数、首次到访时间） | 城市分组 | 那是统计面板的职责。一个功能不承担两个职责 | 按城市归拢 + 展开/收起 |
| 7 | 地图的**拖拽调整地点**、**搜索模糊联想**、**点聚合** | 地图视图 | "搜索选点 + 点击选点"两条路已足够；点聚合是数据量上千才需要的优化 | 搜索选点、点击选点、缩放 |
| 8 | 数据导出的**多格式**（CSV / TXT） | 数据持久化 | 只有 JSON 能完整还原嵌套结构（地点 + 标签列表），CSV 会丢信息 | **仅导出 JSON** |
| 9 | **批量添加地点**、撤销重做 | 新增/编辑弹窗 | 批量补记（导入 CSV）本就排在 Day 8–28 | 单条新增 / 编辑 / 删除 |

**砍项的共同逻辑**：每砍一条，"能跑起来"的时间就少一块，而核心体验（看见停留时长）一点没少。

#### 被砍 / 被移动的功能去向对照（按 shy 要求：不许凭空消失）

| 被处理的功能 | 去向 |
|---|---|
| 点颜色、点形状 | 砍掉（不排期，理由是设计冲突而非工作量） |
| 富文本、标签管理、标签筛选 | 砍掉；若后续有需求，在 Day 8–28 的"交互与视觉打磨"里重估 |
| 时间轴年月分层 | **移到后续**：Day 8–28「时间轴增强」 |
| 城市层级统计数字 | **移到后续**：Day 8–28「统计面板」（见第九节待定项） |
| 地图拖拽 / 模糊联想 / 点聚合 | 砍掉（点聚合留给数据量增长后再评估） |
| CSV / TXT 导出 | 砍掉 |
| 批量添加 | **移到后续**：Day 8–28「批量补记历史 + 数据导入导出」 |

### 4.3 后续（不是砍掉，是排期）

| 阶段 | 内容 |
|---|---|
| **Day 8–28** | 账号与云数据（对应课程 Day 23）→ 照片上传 → 批量补记与数据导入 → **统计面板** → 时间轴增强（按年/月）→ 交互与视觉打磨 |
| **更远期（28 天之外）** | 地点连线语义的进阶玩法、分享卡片、移动端适配 |

---

## 五、页面与页面需求

**本期共 3 个视图 + 1 个弹窗。**（视图＝地图视图、时间轴视图、地点列表视图；弹窗＝5.4 地点详情弹窗；统计面板视图已移至后续，见第九节）

### 5.1 地图视图（默认首屏）

| 项 | 内容 |
|---|---|
| 用途 | 一眼看到自己所有的地点，并通过点的大小看出停留时长 |
| 包含元素 | 可缩放地图；地点点标记；**主城市锚点**（该城市暂无地点时显示为**灰色**）；类型筛选栏；"新增地点"按钮；主城市设置入口 |
| 交互 | 点击地图空白处 → 弹出**新增态**弹窗并带入坐标；悬停点 → 显示地点名与停留时长；点击点 → 打开该地点的**查看态**（只读，详见 5.4） |
| **视觉要求**（shy 特别强调：**美观且清晰**——以下每条都可验证） | ① **停留时长的差异可量化**：最短与最长的点摆在一起，**直径至少相差 2 倍**（「想去」的点固定为最小尺寸，不参与这个映射）；② 点的直径有**上下限**——既不因极值（1 天 vs 10 年）而看不出区别，也不会大到遮住地图；③ 同一城市内相邻的多个点**不完全重叠**，每个点都能被单独点中；④ **线宽差异可量化**：累计停留 1 个月的城市 vs 累计 5 年的城市，线宽**至少相差一倍**；⑤ 线宽有**上下限**——最粗的线不粗成一条带、糊掉地图，最细的线仍可见；⑥ 主城市连线的起点与终点清楚，多条线不糊成一团；⑦ 地图与点标记在全屏和 375 像素窄窗口下都不错位、不溢出 |
| 空状态 | 无任何地点时，地图居中显示提示：「还没有记录。点地图上任意位置，开始记下第一个地方。」 |

### 5.2 时间轴视图

| 项 | 内容 |
|---|---|
| 用途 | 按时间顺序回看自己的人生轨迹 |
| 包含元素 | 地点卡片列表（按停留开始时间倒序）；**不含「想去」类型的地点**（见 4.1 特殊规则）；每张卡片显示：地点名、所属城市、类型、停留时段、正文摘要、标签 |
| 交互 | 点击卡片 → 打开该地点的**查看态**（只读，详见 5.4）；滚动浏览 |
| 空状态 | 「还没有记录。先去地图上添加一个地点。」 |

### 5.3 地点列表视图

| 项 | 内容 |
|---|---|
| 用途 | 按城市归拢查看，适合"我在这座城市生活过哪些地方"的检索需求 |
| 包含元素 | 城市分组（可展开/收起）；每个城市下是该城市的地点条目；显示该城市的地点数量 |
| 交互 | 点击城市名 → 展开/收起；点击地点 → 打开该地点的**查看态**（只读）；点击某城市时地图视图对应点高亮 |
| 空状态 | 无任何地点时显示：「还没有记录。先去地图上添加一个地点。」 |

### 5.4 地点详情弹窗（**三种形态：查看态 / 编辑态 / 新增态**）

同一个弹窗承担三种形态——**目的是让"看一眼"和"改一下"分开，避免用户手滑误改**。

| 项 | 内容 |
|---|---|
| 用途 | ① **查看**一个地点的全部回忆（只读）② **修改**已有地点 ③ **新增**一个地点 |
| **三种形态** | **查看态**（默认）：点击已有的点或卡片进入。只读展示全部已填内容，底部有「编辑」按钮<br>**编辑态**：在查看态点「编辑」进入。字段可改，底部有「保存」「取消」「删除」<br>**新增态**：点击地图空白处进入。字段与编辑态相同，但为空、**没有「删除」** |
| 包含元素 | 地点名称（必填）、所属城市（必填，自动填入可改）、坐标（自动带入）、地点类型（必选）、停留开始时间、停留结束时间、回忆正文、标签输入 |
| 交互 | 查看态：点「编辑」→ 切到编辑态；点关闭 → 直接退出，**不会改动任何内容**<br>编辑态 / 新增态：保存 → 关闭并刷新地图与列表；取消 → 放弃改动；编辑态另有**删除** |
| 校验 | 地点名称、所属城市、地点类型为必填；**只在保存时**校验，未填时提示并阻止保存 |

---

## 六、数据字段

**一个「地点」记录包含以下字段：**

| # | 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| 1 | 地点名称 | 文本 | ✅ | 如"华中科技大学主校区" |
| 2 | 所属城市 | 文本 | ✅ | 如"武汉"。地图选点时自动填入，可手动修改 |
| 3 | 坐标 | 经纬度 | ✅ | 地图点击或搜索选中时自动获得 |
| 4 | 地点类型 | 枚举 | ✅ | `长期居住` / `短期停留` / `旅游` / `想去` |
| 5 | 停留开始时间 | 日期（年-月） | — | 「想去」类型可不填 |
| 6 | 停留结束时间 | 日期（年-月） | — | **可为空**：留空表示"至今仍在" |
| 7 | **停留时长** | 时长 | — | **计算字段，不由用户输入**。结束时间有值时 = 结束 − 开始；结束时间为空时 = 今天 − 开始，**显示为「起始年月 至今」**（例：`2019-09 至今`）。⚠️ **「想去」类型没有起止时间 → 时长为空**，其显示规则见 4.1 下的「关于『想去』类型的特殊规则」 |
| 8 | 回忆正文 | 长文本 | — | 纯文本，支持换行。**本期不支持富文本** |
| 9 | 标签列表 | 文本数组 | — | 支持添加与显示多个标签。**本期不支持标签管理与筛选** |
| 10 | 图片 | 图片数组 | — | ⚠️ **本期只保留字段、不实现上传**（占位为空数组）。目的：Day 8–28 加照片时**无需改动数据结构** |

**全局设置（不属于某条地点记录，单独存储）：**

| 设置项 | 类型 | 说明 |
|---|---|---|
| 主城市 | 文本，**最多 1 个** | 由用户**自选**的城市名。用于计算地图上的放射连线与线宽。**为空时地图上不画任何连线**；**设了但该城市暂无地点时，显示灰色锚点**，该城市出现第一个地点后锚点自动转为正常 |

> **注意**：点的大小是按**数值时长**计算的（"至今"状态取"今天 − 开始时间"），而**显示文案**是「起始年月 至今」。两者不一致是刻意的——否则"至今"的时长永远无法比较。

---

## 七、验收标准（逐条可检查）

**项目：TripMemo（人生记忆地图）** ｜ 文档版本 v1.0 ｜ 日期 2026-09-17 ｜ 本期范围：Day 7 交付 MVP

**每条都能让一个陌生人照着操作、给出"是/否"的结论。**

| # | 对应功能 | 怎么验（操作步骤） | 通过标准 |
|---|---|---|---|
| 1 | 地图打点 | 打开页面 → 在地图任意位置点一下 → 在弹窗里填名称并保存 | 地图上出现一个新点，且所属城市被自动填入 |
| 2 | 地图打点（搜索） | 在搜索框输入一个城市名 → 选中结果 → 保存 | 新点出现在该地名位置 |
| 3 | 地点类型 | 依次创建"长期居住/短期停留/旅游/想去"四个地点 → 在筛选栏点"想去" | 地图上**只剩**"想去"的那一个点 |
| 4 | 点的视觉差异 | 创建两个地点：一个停留 3 个月，一个停留 3 年 | 两者都在地图上，且**停留 3 年的点直径至少是前者的 1.5 倍** |
| 5 | 点的视觉差异（悬停） | 鼠标悬停到任意点上 | 弹出提示，含地点名与停留时长 |
| 6 | 回忆表单 | 填满全部可选字段并保存 → 重新打开该地点 | 已填内容全部回显，无字段丢失 |
| 7 | 回忆表单（必填校验） | 只填"地点名称"，其余留空 → 点保存 | 保存被阻止，并提示缺少哪些必填项 |
| 8 | 停留时长计算 | 创建地点：开始 2019-09，结束留空 | 该地点显示的停留时长为 **`2019-09 至今`**（不是天数数字、不是空） |
| 9 | 城市分组 | 创建 3 个地点，其中 2 个在同一个城市 → 打开地点列表视图 | 显示为 2 个城市分组，同城的那组含 2 条 |
| 10 | 城市分组（交互） | 点击城市名 | 该分组展开/收起；地图上对应点高亮 |
| 11 | 时间轴 | 创建 3 个地点，停留开始时间分别为 2010、2015、2020 → 打开时间轴视图 | 顺序为 2020 → 2015 → 2010（倒序） |
| 12 | 数据持久化 | 添加若干地点 → **刷新页面** | 所有地点与内容仍在 |
| 13 | 数据持久化（重开） | 关闭浏览器 → 重新打开页面 | 所有数据仍在 |
| 14 | 数据持久化（导出） | 点「导出 JSON」 | 浏览器下载一个 `.json` 文件，内容包含全部地点与其字段 |
| 15 | 地点连线（主城市自选） | 把某个城市设为「主城市」→ 另建一个在其他城市的地点 | 地图上出现一条从主城市指向**该城市**的线 |
| 16 | 地点连线（按城市聚合） | 在同一个非主城市下建 3 个地点 | 通向该城市**仍然只有一条线**，不是三条 |
| 17 | 地点连线（主城市内的点） | 在主城市内再建一个地点 | **不产生新的连线** |
| 18 | 地点连线（线宽随时长） | 建两个城市：A 累计停留 1 个月，B 累计停留 5 年 | 连向 B 的线**至少比连向 A 的粗一倍** |
| 19 | 地点连线（线宽上下限） | 制造极端数据（1 天 vs 20 年） | 最粗的线**不粗成一条带**、最细的线**仍可见** |
| 20 | 地点连线（换主城市） | 把主城市换成另一个城市 | 全部连线自动重算，从新主城市出发 |
| 21 | 地点连线（未设主城市） | 清空主城市设置 | 地图上**没有任何连线**，但所有点照常显示 |
| 22 | 地点连线（主城市暂无地点） | 把一个**没有任何地点**的城市设为主城市 | 地图上显示一个**灰色锚点**，且**不画任何线** |
| 23 | 地点连线（锚点转正常） | 给该主城市添加第一个地点 | 灰色锚点**自动变为正常状态**，并开始向其他城市画线 |
| 24 | 地图视觉（点大小可辨） | 创建两个点：一个停留 1 天、一个停留 10 年 → 看地图 | 两个点的**直径至少相差 2 倍**，且较大的点**不遮挡**其它点 |
| 25 | 地图视觉（重叠可点） | 在城市里同一位置附近创建 3 个地点 | **分别点击每个点，每次选中的都是被点的那个** |
| 26 | 地图视觉（窄窗口） | 把浏览器窗口缩到 **375 像素宽**（手机宽度） | 地图与点标记不错位、不溢出 |
| 27 | 空状态 | 清空所有数据后打开页面 | **地图视图、时间轴视图、地点列表视图**均显示对应的空状态提示文案 |
| 28 | 删除 | 在编辑弹窗中点删除 | 该地点从地图与所有列表中消失，刷新后依然不存在 |
| 29 | 边界（无越界） | 通读 PRD 全文 | **不出现任何具体技术选型**（框架名、地图服务名、数据库名、托管平台名） |
| 30 | 「想去」的显示 | 创建一个类型为「想去」的地点 | 该点显示为**最小尺寸**，且**不会随停留时长变大** |
| 31 | 「想去」的悬停文案 | 鼠标悬停到一个「想去」的点上 | 显示「**下一站**」，**不显示停留时长** |
| 32 | 「想去」与时间轴 | 打开时间轴视图（已存在「想去」类型的地点） | 时间轴里**看不到**任何「想去」的地点 |
| 33 | **查看态与编辑态分离** | 点击一个**已存在**的地点（地图上的点、时间轴卡片、或地点列表条目） | 打开的是**只读的查看态**——内容不可编辑；点「编辑」后才进入可改的编辑态 |

---

## 八、边界与约束

1. **本期不做后端服务**：用户数据只存在浏览器本地，**不上传到任何服务器**，不做账号。
   （**页面运行需要联网**——地图底图与地名搜索依赖在线地图服务。这是"运行依赖"，不等于"把用户数据传上去"，两者要分清。）
2. **不做自动定位**：本产品所有地点均由用户手动添加（Web 页面无法在锁屏后持续定位）。
3. **不做社交**：无分享、无公开、无评论。
4. **不写技术选型**：框架、地图服务、存储实现均属 Day 5。
5. **不含任何代码**。

---

## 九、待定问题

| # | 待定项 | 影响 | 需谁定 |
|---|---|---|---|
| 1 | 线宽的**上下限具体取值**（细到多少、粗到多少） | 只影响视觉，不影响逻辑 | Day 7 调视觉效果时定 |
| 2 | 「统计面板」在 Day 8–28 的具体范围 | 本期不写 | Day 8–28 再定 |
| 3 | 图片字段的数据格式（本地路径 / Base64 / 云端 URL） | 本期不实现，但影响 Day 8–28 的结构设计 | Day 8–28 再定 |

> **已确定项（2026-09-17 由 shy 定，不再待定）**：
> ① 主城市**只有一个，由用户自选**（18:57 修订，原为"最多两个"）；② 连线**按城市聚合**——一个非主城市**一条**线，不是每个地点一条；③ **线宽 ∝ 该城市的累计停留时长**，且设上下限；④ 「去的次数」不编码到线上（用"同城多个点"表达）；⑤ 主城市内的地点不连线；⑥ 「至今」显示格式为 **`起始年月 至今`**（例 `2019-09 至今`）；⑦ **主城市设了但暂无地点时显示灰色锚点**，该城市出现第一个地点后锚点自动转为正常；⑧ **「想去」的三条特殊规则**：点固定为最小尺寸不参与大小映射 / 悬停显示「**下一站**」且不显示时长 / **不进入时间轴**。

---

## 附：本 PRD 与上游文档的关系

| 文档 | 回答的问题 | 状态 |
|---|---|---|
| `research.md` | 为什么做？别人怎么做的？ | ✅ 已完成（2026-09-17） |
| **本文件 `PRD.md`** | **做成什么样？什么算做完？** | ← 当前 |
| 技术选型 | 用什么做？ | Day 5 |
| `index.html` 与代码 | 做出来 | Day 7 起 |
